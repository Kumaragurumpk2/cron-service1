{
  "name": "nestjs-cron-service",
  "version": "1.0.0",
  "description": "A NestJS service for managing cron jobs with MongoDB",
  "main": "dist/main.js",
  "scripts": {
    "start": "nest start",
    "build": "nest build",
    "test": "jest"
  },
  "dependencies": {
    "@nestjs/common": "^9.0.0",
    "@nestjs/core": "^9.0.0",
    "@nestjs/mongoose": "^9.0.0",
    "@nestjs/schedule": "^2.0.0",
    "@nestjs/throttler": "^2.0.0",
    "mongoose": "^6.0.0"
  }
}